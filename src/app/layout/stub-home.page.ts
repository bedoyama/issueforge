import { Component } from '@angular/core';

@Component({
  selector: 'forge-stub-home',
  templateUrl: './stub-home.page.html',
  styles: `
    section { padding: var(--space-8); }
    p { color: var(--text-muted); margin-top: var(--space-2); }
  `,
})
export class StubHomePage {}
